import styled from "styled-components"

export const LogoBlock = styled.a.attrs({
  href: "/"
})`
  display: flex;
  flex-direction: row;
  align-items: center;
  outline: none;
  text-decoration: none;
  color: inherit;
  user-select: none;
`
