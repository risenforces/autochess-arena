import styled from "styled-components"

export const LogoBlock = styled.a`
  display: flex;
  flex-direction: row;
  align-items: center;
  user-select: none;
  cursor: pointer;
  text-decoration: none;
  color: inherit;
`
